---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="Safe Canary Test - Softprobe"
  description="Catch critical errors safely in an isolated replay environment."
>
  <div class="debug-page canary-page">
    <section class="debug-hero">
      <div class="debug-copy">
        <p class="debug-kicker">Softprobe Canary</p>
        <h1>Safe Canary Test</h1>
        <p>Catch critical errors safely in an isolated replay environment.</p>
      </div>

      <div class="debug-visual-wrap">
        <div class="canary-hero-diagram">
          <div class="canary-env canary-env-prod">
            <p class="canary-env-title">Production Environment</p>
            <div class="canary-env-grid">
              <div class="canary-traffic-source" aria-label="Incoming traffic">⇢</div>

              <div class="canary-node canary-node-booking">
                <span class="canary-node-icon">running</span>
                <div class="canary-node-head canary-node-head-primary">
                  <span class="canary-glyph canary-glyph-lg" aria-hidden="true">
                    <svg viewBox="0 0 24 24" fill="none">
                      <rect x="3" y="4" width="18" height="6" rx="2"></rect>
                      <rect x="3" y="14" width="18" height="6" rx="2"></rect>
                      <circle cx="7" cy="7" r="1.2"></circle>
                      <circle cx="7" cy="17" r="1.2"></circle>
                    </svg>
                  </span>
                </div>
                <p class="canary-node-label">Booking Service</p>
              </div>

              <div class="canary-stack">
                <div class="canary-node canary-node-payment">
                  <div class="canary-node-head canary-node-head-primary">
                    <img class="canary-brand-img" src="https://cdn.simpleicons.org/stripe/635BFF" alt="Stripe" width="28" height="28" />
                  </div>
                  <p>Stripe</p>
                </div>
                <div class="canary-node canary-node-db">
                  <div class="canary-node-head canary-node-head-primary">
                    <img class="canary-brand-img" src="https://cdn.simpleicons.org/postgresql/4169E1" alt="PostgreSQL" width="28" height="28" />
                  </div>
                  <p>PostgreSQL</p>
                </div>
              </div>

              <svg class="canary-arrows canary-arrows-prod" viewBox="0 0 520 200" aria-hidden="true">
                <path d="M0 95 C 58 95, 72 95, 96 95" />
                <path d="M160 84 C 246 74, 276 56, 362 40" />
                <path d="M160 100 C 246 118, 276 136, 362 148" />
              </svg>

              <div class="canary-flow-labels" aria-hidden="true">
                <div class="canary-flow-chip canary-flow-chip-booking-req">card=4238... order_id=88421</div>
                <div class="canary-flow-chip canary-flow-chip-booking-res">paid=$120</div>
              </div>
            </div>
          </div>

          <div class="canary-replay-arrow"><span>replay captured traffic</span></div>

          <div class="canary-env canary-env-sandbox">
            <p class="canary-env-title">Sandbox Environment</p>
            <div class="canary-env-grid canary-env-grid-sandbox">
              <div class="canary-node canary-node-booking canary-node-booking-new">
                <span class="canary-node-icon canary-node-icon-blue">vNext</span>
                <div class="canary-node-head canary-node-head-primary">
                  <span class="canary-glyph canary-glyph-lg" aria-hidden="true">
                    <svg viewBox="0 0 24 24" fill="none">
                      <rect x="3" y="4" width="18" height="6" rx="2"></rect>
                      <rect x="3" y="14" width="18" height="6" rx="2"></rect>
                      <circle cx="7" cy="7" r="1.2"></circle>
                      <circle cx="7" cy="17" r="1.2"></circle>
                    </svg>
                  </span>
                </div>
                <p class="canary-node-label">Booking Service (new)</p>
              </div>

              <div class="canary-stack canary-injected">
                <div class="canary-node canary-node-captured">
                  <div class="canary-node-head canary-node-head-primary">
                    <img class="canary-brand-img" src="https://cdn.simpleicons.org/stripe/635BFF" alt="Stripe" width="28" height="28" />
                  </div>
                  <p>Auto Mock</p>
                </div>
                <div class="canary-node canary-node-captured">
                  <div class="canary-node-head canary-node-head-primary">
                    <img class="canary-brand-img" src="https://cdn.simpleicons.org/postgresql/4169E1" alt="PostgreSQL" width="28" height="28" />
                  </div>
                  <p>Auto Mock</p>
                </div>
              </div>

              <svg class="canary-arrows canary-arrows-sandbox" viewBox="0 0 520 200" aria-hidden="true">
                <path d="M0 95 C 58 95, 72 95, 96 95" />
                <path d="M362 40 C 276 56, 246 74, 160 84" />
                <path d="M362 148 C 276 136, 246 118, 160 100" />
              </svg>

              <div class="canary-flow-labels" aria-hidden="true">
                <div class="canary-flow-chip canary-flow-chip-booking-req">card=4238... order_id=88421</div>
                <div class="canary-flow-chip canary-flow-chip-booking-res" style="color: red;">paid=$150</div>
              </div>
            </div>
            <p class="canary-inject-note">Dependencies are injected from captured traffic. No real upstream calls.</p>
          </div>
        </div>
      </div>
    </section>

    <header class="section-head">
      <p class="section-kicker">Softprobe AI</p>
      <h2 class="section-title">Rollout with absolute confidence</h2>
      <p class="section-subtitle">Canary test every release without any customer impact.</p>
    </header>

    <section class="debug-confidence">
      <div class="debug-confidence-media">
        <div class="debug-confidence-media-inner">
          <article class="canary-replay-card">
            <div class="canary-replay-icon-item"><span class="canary-icon-tile">↔</span><p>Mirroring live traffic</p></div>
            <div class="canary-replay-icon-item"><span class="canary-icon-tile"><img class="canary-brand-img canary-brand-img-sm" src="https://cdn.simpleicons.org/stripe/635BFF" alt="Stripe" width="16" height="16" /></span><p>Replay upstream</p></div>
            <div class="canary-replay-icon-item"><span class="canary-icon-tile"><img class="canary-brand-img canary-brand-img-sm" src="https://cdn.simpleicons.org/postgresql/4169E1" alt="PostgreSQL" width="16" height="16" /></span><p>Replay captured responses</p></div>
          </article>
        </div>
      </div>

      <div class="debug-confidence-copy debug-description-panel debug-description-panel--right">
        <p class="debug-panel-kicker">Release gating</p>
        <h2>Replay with 100% prod data</h2>
        <p>Validate release behavior against real production cases before rollout.</p>
      </div>
    </section>

    <section class="section debug-artifact-section" aria-label="Response validation">
      <div class="action-grid debug-artifact-grid">
        <div class="action-copy debug-artifact-copy-wrap">
          <div class="debug-artifact-sticky">
            <div class="debug-artifact-copy debug-description-panel debug-description-panel--left">
              <p class="debug-panel-kicker">Comparison engine</p>
              <h2>AI validates all responses</h2>
              <p>Softprobe compares production and replay JSON payloads field by field and blocks mismatches.</p>
            </div>
          </div>
        </div>

        <div class="action-visual debug-artifact-visual">
          <div class="action-visual-inner">
            <article class="slack-post canary-json-card">
              <p class="canary-json-title">Detected response difference: tax calculation</p>
              <div class="canary-json-col canary-json-col-diff">
                <p>response.diff</p>
                <pre class="canary-json-diff"><code><span class="diff-line diff-line-ctx">&#123;</span>
<span class="diff-line diff-line-ctx">  "subtotal": 120.00,</span>
<span class="diff-line diff-line-del">- "tax": 10.80,</span>
<span class="diff-line diff-line-del">- "total": 130.80,</span>
<span class="diff-line diff-line-add">+ "tax": 12.00,</span>
<span class="diff-line diff-line-add">+ "total": 132.00,</span>
<span class="diff-line diff-line-ctx">  "currency": "USD"</span>
<span class="diff-line diff-line-ctx">&#125;</span></code></pre>
              </div>
            </article>
          </div>
        </div>
      </div>
    </section>

    <section class="section debug-incident-section" aria-label="No customer impact controls">
      <div class="action-grid">
        <div class="action-visual debug-incident-visual">
          <div class="action-visual-inner">
            <article class="slack-post canary-safety-visual">
              <div class="canary-lane canary-lane-prod"><p>✓ Production traffic path</p><span>customers served</span></div>
              <div class="canary-lane canary-lane-replay"><p>◎ Replay isolation path</p><span>sandbox only</span></div>
              <div class="canary-lane canary-lane-gate"><p>⛔ Release gate</p><span>paused on mismatch</span></div>
            </article>
          </div>
        </div>

        <div class="action-copy debug-incident-copy debug-description-panel debug-description-panel--right">
          <p class="debug-panel-kicker">Safety first</p>
          <h2>No customer impact</h2>
          <p>Traffic stays in production path while replay runs in isolation and unsafe releases remain paused.</p>
        </div>
      </div>
    </section>

    <section class="canary-coverage">
      <p class="canary-coverage-line">100% production case coverage. 0% customer risk.</p>
      <ul>
        <li>Captures end-to-end message bodies</li>
        <li>Replays real cases against test sandbox</li>
        <li>Injects captured upstream mocks safely</li>
        <li>Validates production-level accuracy</li>
      </ul>
    </section>
  </div>
</BaseLayout>

<style>
  .canary-hero-diagram { border: 1px solid #dbe4f0; border-radius: 24px; padding: 16px; background: linear-gradient(150deg,#f8fbff 0%,#f5f7fb 100%); box-shadow: 0 20px 48px rgba(15,23,42,.14); }
  .canary-env { border: 1px solid #d6e2ef; border-radius: 16px; padding: 12px; }
  .canary-env-title { margin: 0; font-size: .82rem; letter-spacing: .07em; text-transform: uppercase; color: #0f766e; font-weight: 900; }
  .canary-env-prod { background: linear-gradient(145deg,#ebf5ff 0%,#ecfeff 100%); }
  .canary-env-sandbox { margin-top: 12px; background: linear-gradient(145deg,#f0fdf4 0%,#f8fce9 100%); }
  .canary-env-grid { margin-top: 10px; position: relative; display: grid; grid-template-columns: 1fr 1fr; gap: 12px; min-height: 170px; }
  .canary-env-grid-sandbox { min-height: 130px; }

  .canary-traffic-source { position: absolute; left: 8px; top: 62px; width: 36px; height: 36px; border: 1px solid #bfdbfe; border-radius: 999px; background: #eff6ff; display: flex; align-items: center; justify-content: center; color: #1d4ed8; font-weight: 900; }
  .canary-stack { display: grid; gap: 10px; align-content: start; }
  .canary-node { position: relative; border: 1px solid #cfdae7; border-radius: 12px; padding: 10px; background: #fff; display: grid; justify-items: center; align-content: center; gap: 7px; text-align: center; }
  .canary-node-booking { min-height: 126px; }
  .canary-node-label { margin: 0; font-size: .74rem; color: #1e293b; font-weight: 800; }

  .canary-node-head-primary { width: 74px; height: 74px; border-radius: 999px; border: 1px solid #c8d6ea; background: #fff; box-shadow: 0 8px 20px rgba(15,23,42,.08); display: flex; align-items: center; justify-content: center; }
  .canary-glyph { width: 42px; height: 42px; color: #2563eb; display: inline-flex; }
  .canary-glyph svg { width: 42px; height: 42px; stroke: currentColor; fill: none; stroke-width: 1.55; }

  .canary-brand-icon { width: 30px; height: 30px; border-radius: 999px; font-size: .68rem; font-weight: 900; display: inline-flex; align-items: center; justify-content: center; }
  .canary-brand-icon-stripe { background: #eef2ff; color: #4338ca; border: 1px solid #c7d2fe; }
  .canary-brand-icon-db { background: #eff6ff; color: #1d4ed8; border: 1px solid #bfdbfe; }
  .canary-brand-img { width: 28px; height: 28px; display: block; }
  .canary-brand-img-sm { width: 16px; height: 16px; }

  .canary-node-icon { position: absolute; top: 8px; right: 8px; font-size: .66rem; padding: 2px 8px; border-radius: 999px; background: #dcfce7; border: 1px solid #bbf7d0; color: #166534; font-weight: 800; }
  .canary-node-icon-blue { background: #dbeafe; border-color: #bfdbfe; color: #1e3a8a; }

  .canary-node-payment { border-color: #c7d2fe; background: linear-gradient(145deg,#f5f3ff 0%,#eef2ff 100%); }
  .canary-node-db { border-color: #bfdbfe; background: linear-gradient(145deg,#eff6ff 0%,#f0f9ff 100%); }
  .canary-node-captured { border-style: dashed; border-color: #c4d7c6; background: #fbfff7; }

  .canary-arrows { position: absolute; inset: 0; width: 100%; height: 100%; pointer-events: none; }
  .canary-arrows path { fill: none; stroke-linecap: round; stroke-linejoin: round; stroke-width: 2.8; }
  .canary-arrows-prod path { stroke: #2563eb; }
  .canary-arrows-sandbox path { stroke: #16a34a; stroke-dasharray: 6 6; }

  .canary-flow-labels { position: absolute; inset: 0; pointer-events: none; z-index: 2; }
  .canary-flow-chip { position: absolute; border: 1px solid; border-radius: 6px; width: 108px; padding: 3px 7px; font-size: .64rem; line-height: 1.2; box-shadow: 0 8px 18px rgba(15,23,42,.12); }
  .canary-flow-chip-booking-req { left: 1%; top: 31%; background: #eef2ff; border-color: #c7d2fe; color: #312e81; }
  .canary-flow-chip-booking-res { left: 4%; top: 52%; background: #eef2ff; border-color: #c7d2fe; color: #312e81; }
  .canary-flow-chip-payment-req { left: 44%; top: 20%; background: #eef2ff; border-color: #c7d2fe; color: #312e81; }
  .canary-flow-chip-payment-res { left: 66%; top: 17%; background: #eef2ff; border-color: #c7d2fe; color: #312e81; }
  .canary-flow-chip-sql-req { left: 44%; top: 60%; background: #eff6ff; border-color: #bfdbfe; color: #1e3a8a; }
  .canary-flow-chip-sql-res { left: 6%; top: 74%; background: #eff6ff; border-color: #bfdbfe; color: #1e3a8a; }
  .canary-flow-chip-warn { background: #ef4444; border-color: #dc2626; color: #ffffff; }
  .canary-flow-chip-attn { background: #f59e0b; border-color: #d97706; color: #ffffff; }

  .canary-replay-arrow { margin-top: 10px; border: 1px solid #bbf7d0; border-radius: 12px; background: #ecfdf5; padding: 8px 10px; text-align: center; color: #166534; font-size: .8rem; font-weight: 800; }
  .canary-inject-note { margin: 10px 0 0; color: #3f6212; font-size: .82rem; font-weight: 700; }

  .canary-replay-card { padding: 22px; }
  .canary-replay-icon-item { display: flex; align-items: center; gap: 10px; margin-top: 10px; }
  .canary-replay-icon-item:first-child { margin-top: 0; }
  .canary-icon-tile { width: 34px; height: 34px; border: 1px solid #dbe4f0; border-radius: 8px; background: #fff; display: inline-flex; align-items: center; justify-content: center; color: #2563eb; font-size: .72rem; font-weight: 900; }
  .canary-replay-icon-item p { margin: 0; font-weight: 900; color: #0f172a; }

  .canary-json-title { margin: 0; font-size: .92rem; color: #0f172a; font-weight: 900; }
  .canary-json-col { margin-top: 12px; border: 1px solid #e5e7eb; border-radius: 12px; background: #f8fafc; padding: 10px; }
  .canary-json-col p { margin: 0; font-size: .72rem; letter-spacing: .06em; text-transform: uppercase; color: #475569; font-weight: 900; }
  .canary-json-col pre { margin: 8px 0 0; font-size: 12px; line-height: 1.55; color: #0f172a; white-space: pre; overflow-x: auto; font-family: ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono","Courier New",monospace; }
  .diff-line { display: block; padding: 1px 8px; border-radius: 6px; }
  .diff-line-ctx { color: #0f172a; }
  .diff-line-del { color: #991b1b; background: #fef2f2; border: 1px solid #fecaca; }
  .diff-line-add { color: #166534; background: #f0fdf4; border: 1px solid #bbf7d0; }

  .canary-safety-visual { width: min(760px,100%); display: grid; gap: 10px; }
  .canary-lane { border-radius: 12px; border: 1px solid #e5e7eb; padding: 12px; display: flex; justify-content: space-between; gap: 10px; }
  .canary-lane p { margin: 0; font-size: .86rem; font-weight: 800; color: #0f172a; }
  .canary-lane span { font-size: .72rem; font-weight: 900; border-radius: 999px; padding: 5px 10px; }
  .canary-lane-prod { background: #f0fdf4; border-color: #bbf7d0; }
  .canary-lane-prod span { background: #dcfce7; color: #166534; }
  .canary-lane-replay { background: #eff6ff; border-color: #bfdbfe; }
  .canary-lane-replay span { background: #dbeafe; color: #1e3a8a; }
  .canary-lane-gate { background: #fef2f2; border-color: #fecaca; }
  .canary-lane-gate span { background: #fee2e2; color: #991b1b; }

  .canary-coverage { margin-top: 14px; border: 1px solid #dbe4f0; border-radius: 18px; background: linear-gradient(150deg,#f8fafc 0%,#f0fdf4 100%); padding: 24px; }
  .canary-coverage-line { margin: 0; font-size: clamp(1.24rem,2.6vw,1.9rem); font-weight: 900; color: #0f172a; }
  .canary-coverage ul { margin: 14px 0 0; padding-left: 20px; color: #334155; line-height: 1.7; }

  @media (max-width: 900px) {
    .canary-env-grid { grid-template-columns: 1fr; }
    .canary-flow-chip { width: 92px; font-size: .58rem; }
  }
</style>
